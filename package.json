{
  "name": "photo-game-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-manifest": "node generate-manifest.js",
    "build": "npm run generate-manifest && react-scripts build",
    "start": "react-scripts start"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  }
}
